[project]
name = "videoq-mcp"
version = "0.1.0"
description = "VideoQ MCP bridge"
requires-python = ">=3.9"

[project.scripts]
videoq-mcp = "videoq_mcp_server:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
